# 工作站版本发布验证

验证日期：2026-09-13。环境：Ubuntu-20.04 / Python 3.11.13。此记录对应新增的工作站接入流程；既有统计数值验证见`validation_report.md`。

- `ruff check src tests`通过。
- `pytest -q`：48项通过，包括原有34项和14项新增检查。
- 公共SAS文件验证未压缩、压缩、日期时间和特殊缺失的读取；不是CNSR-III患者文件。
- 新检查覆盖字段存在但全部缺失、非法编码、重复ID、前导零、大小写、来源歧义、数值ID特殊缺失、影像精确连接、QC未审核、两步配置以及停止阶段。
- 700例合成数据通过`run --through prepare`，完成临床审计、CSV入口、影像连接和病例准备。
- 同一合成数据通过`run --through report --hypothesis H1`，验证从审计到H1模型、敏感性分析及HTML报告的完整命令入口。
- 已测试临床审计无需影像目录、审计失败停止后续步骤、默认准备阶段不调用统计模型。

未在本机读取真实患者SAS或连接真实WMH目录。实际编码、文件间覆盖、患者数、影像路径布局和模型可估计性，在工作站接入后依据审计结果确定。上述通过状态仅代表软件检查。

## Conda兼容性验证（2026-09-13）

- 使用Conda 25.7.0按`environment.yml`从头创建独立Linux环境，Python 3.11.16；原有uv环境保留。
- 33个适用于Linux的Python依赖版本与`uv.lock`一致，`pip check`通过。
- 在激活的Conda环境中，`ruff check src tests`通过，`python -m pytest -q`为48项通过。
- 直接调用`wmh-hcy`命令成功；700例合成数据完成`run --through prepare`，不需要`uv run`。
- 环境文件、固定依赖清单和操作说明已纳入仓库；验证环境和数据输出不纳入Git。

## MRI时间字段移除（2026-09-16）

- 按研究者决定，删除发病至MRI天数字段；临床白名单由39项改为38项。MRI时间不再参与审计、数据标准化或入组。
- 主分析以发病日为时间尺度，从实际基线采血日进入风险集。采血后、3个月内发生的复发仍计入；采血前及同日事件不进入主分析。
- 新增队列版本记录；更新前已生成的队列需重新执行prepare，避免沿用旧入组时间。
- Ubuntu-20.04 / Python 3.11.13：ruff通过，pytest为55项通过。
- 新测试覆盖无MRI时间字段的四份SAS白名单提取与合并（模拟读取后端），以及审计、队列准备、早期事件、非法采血时间和旧队列拦截。公共真实SAS格式读取测试继续通过。
- 700例合成数据完成全部12个分析模块，模块状态均为ESTIMATED；H2与H3绝对风险计算及HTML报告生成成功。演示使用2份插补，仅用于运行验证。
- 同一合成数据审计为38/38项、READY_FOR_EXTRACTION。未在本机读取真实患者数据或计算真实队列例数。
- 28页方法学PPT同步修改入组时间与字段数量；最终文件重新导入、全页渲染及包结构检查通过，修改页已目视核查。既有第11页连接线层级提示不涉及本次修改，目视未见遮挡。
- 本次未修改依赖；工作站可继续使用既有Conda环境。

## 取消人工影像质控入组门槛（2026-09-16）

- 按研究者要求，主队列、T1子分析及急性病灶扩展均取消人工质控状态筛选。保留精确ID连接和体积数值可用性检查。
- 不读取外部质控表；旧配置的require_qc和qc_csv不再参与执行。既有影像CSV中的审核标签保留为元数据，不改写成通过。
- 图像审计输出改为wmh_icv_eligible_matched、t1_available_matched及acute_lesion_available_matched；不再把数据可用人数命名为质控通过人数。
- 新增影像入组规则版本记录；旧队列需重新生成。run --through report会自动重建队列并开始H1–H4分析。
- Ubuntu-20.04 / Python 3.11.13：ruff通过，61项测试通过。覆盖无审核列、unreviewed/fail/stale标签、旧配置、失效外部审核表，以及体积缺失/负数/无穷值和零体积处理。
- 700例无质控列的合成数据完成12个分析模块，均为ESTIMATED，HTML报告生成成功。采用演示重复次数，属于软件验证；未在本机运行真实患者统计。
- 28页方法学PPT同步修改数据接入及T1说明；重新导入、包结构和布局检查通过，修改页已目视核查。
- 分析依赖未改变，既有Conda环境继续兼容。

## 截图用诊断摘要（2026-09-17）

- 新增`scripts/diagnose_results.py`，只读取汇总JSON和模型阈值诊断表，将既有结果压缩为两页终端输出；不导入分析包、不读取患者CSV、不重跑模型、不修改结果。
- 新增8项独立测试并全部通过，覆盖失败类型汇总、原始错误文本隐藏、诊断缺失、插补轨迹、模型矩阵诊断、阈值诊断、结果路径迁移及只读性。
- 本次测试环境为Windows / Python 3.13.5；没有在本机重跑工作站的Python 3.11患者分析。Ruff 0.12.12按项目Python 3.11语法目标检查新增脚本和测试，通过。
- 合成汇总文件验证两页命令输出，分别23行和24行，最长95字符；执行前后输入文件逐字节一致。这是软件验证，不是患者结果。
- 未修改分析模型、变量、队列规则或依赖锁定；工作站更新后直接运行脚本，无需重装Conda环境或重新分析。

## 2—5年随访扩展（2026-09-17）

- 按研究者截图新增28个可选字段：各年IS、STROKE、HS及相应天数，加24/36/48/60月mRS。原38项一年白名单规则保留，新字段缺失不成为一年分析的必需条件。
- 新增独立`longterm`入口；各年H2/H3/H4/T1分别建立病例清单，采用实际事件/删失天数，不将未复发者补为完整随访；保留mRS 6，缺失mRS不推成死亡。
- 86项测试通过；Ruff检查通过。新增检查覆盖多SAS来源及大小写、每年字段选择、行政截止、提前删失、采血前/同日事件、三个月进入风险集、跨年首次事件矛盾、功能队列独立性、长期mRS插补、固定四年Holm、原一年结果指针保护及截图摘要。
- 本次使用Windows / Python 3.11.16，并按`uv.lock`安装固定依赖。综合测试中发现Windows的NumPy longdouble精度缓存与statsmodels数值Hessian的进程内导入顺序问题；有序模型数值测试在独立解释器中执行并通过，不修改统计估计器或依赖版本。未声称本次完成Linux整套环境复验。
- 600例合成数据完成4年×4模型，共16个模型均ESTIMATED；2份插补用于软件验证，真实配置仍为50份。HTML、汇总表、森林图及两页终端摘要生成成功，森林图已目视检查。
- 长期死亡日期未提供，因此没有生成2—5年竞争风险绝对发生概率。长期结果位于独立目录，原一年模型结果不改写。没有读取真实患者SAS或产生真实研究结果。

## 死亡模型定向诊断（2026-09-17）

- 在截图摘要中增加`--page 3`。本地读取prepared患者行，按精确ID、进入/结束时间、事件类型及保存模型人数核对批次，然后只显示预设分类变量的总人数、缺血和死亡事件数。不同批次、重复ID或无效输入会停止该模型的分类汇总。
- 输出不包含患者ID、原始异常文本或未知分类取值；不联网、不写结果、不拟合模型。前两页继续仅读取汇总文件。第三页明确区分当前未插补协变量与历史模型批次，不将零事件类别自动判断为分离或删除依据。
- Windows / Python 3.11.16：96项测试通过，Ruff通过。新增10项检查覆盖汇总正确性、只读性、ID和异常值隐藏、时间/事件/成员/人数不一致、重复ID、前导零、输入缺失及长期入口隔离。
- 没有改变统计模型、缺失值处理、bootstrap成功门槛或任何分析依赖；没有在本机重新运行患者分析。工作站更新代码后可直接运行，无需重装Conda环境。
