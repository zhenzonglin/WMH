# 第四版修订与验证

修订日期：2026-09-18。版本：`imaging_four_studies_20260918_v4`。依据研究者明确指令，IMG_ICAS和Apo_AI不作为本轮分析内容。这是查看字段审计后的方案修订，不称为原始预注册内容，也不依据效应或P值作变量筛选。

## 本次修改

- 删除IMG_ICAS字段及其内部变量icas，恢复期血压的主要模型、双臂平均血压、12个月独立起点、时间变化和完整病例分析均取消该调整项。
- 删除Apo_AI字段及其内部变量apo_ai，不再安排CEC的Apo_AI扩展模型。CEC主模型仍调整HDL-C，与不调整HDL-C的M0模型继续使用相同病例、尺度及完成数据。
- 两字段不再参与新运行的字段提取、协变量插补、设计矩阵、字段缺失诊断或候选名称查找。不采用替代变量，也不因原表中有这两列而重新纳入。
- CEC研究内次要分析的BH-FDR家庭相应移除该扩展；四项主要假设和总汇报四项Holm家庭不变。原始主要CEC值的异常核查仍保留。
- 同步中文统计方案、Word、字段表、协变量角色表和工作站说明。旧版方案、旧报告及旧运行目录保留。当前汇总将旧版本结果标为PREVIOUS_VERSION，不与v4混合。

## 验证

- 全套161项测试通过，ruff检查通过。
- 新增回归检查确认：两字段缺失或含非法内容均不影响BP、CEC队列和准备状态；不进入任何当前研究的主要变量集合或角色表；诊断不再提示补齐；CEC不再安排Apo_AI模型，HDL-C仍保留。
- 旧版结果的隔离及固定四项Holm校正测试通过。
- 使用2400例明确标注为合成的随机记录，运行BP和CEC完整流程；BP 5项、CEC 8项共13项分析均为ESTIMATED，报告生成成功，未安排Apo_AI模型。该流程检查使用2份插补、2次迭代以缩短验证时间；真实默认仍为50份、10次迭代，不将本次演示当作精度或真实效应证据。
- 中文Word共15页，逐页渲染检查完成。打包的LibreOffice不可用，改由本机Word导出PDF，再使用绑定运行时栅格化检查，未发现表格溢出或文字裁切。

本机验证使用合成数据。真实工作站的人数、结局、缺失和模型结果须重新审计、拟合后报告，本节不预填或沿用旧版患者结果。

## 已采用精简代码目录的工作站

先更新，成功后再执行审计：

```bash
git -C /data/usersdir/linzhenzong/WMH-code-v3 pull --ff-only
```

```bash
cd /data/usersdir/linzhenzong/WMH
wmh-study audit --study all
wmh-study diagnose --page 3
```

代码目录名称WMH-code-v3无需随方案版本改名。已采用editable安装时，本次Python代码修改不需要重新安装依赖。每次审计创建新快照；旧输入和结果不覆盖。CEC若仍为REVIEW_REQUIRED，应继续核对其异常类型，不能因删除Apo_AI而绕过主要暴露检查。
