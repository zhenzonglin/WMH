# 软件验证报告

日期：2026-09-12。此报告验证软件和合成数据流程；不是CNSR-III患者研究结果。

## 当前确认状态

- WSL Ubuntu-20.04内运行，Python 3.11.13，独立uv环境及uv.lock。
- pytest：34项，失败0，错误0。
- Ruff代码检查通过。
- 真实配置doctor、extract、prepare均返回退出码2，明确提示真实输入未配置。
- 合成数据：12个分析模块均完成；主要竞争风险曲线及参考点区间可生成。
- 本次合成结果位置：`outputs/demo/results/20260912T155221186342Z`。

## 验证范围

1. 4份pyreadstat公共SAS样例：普通/压缩SAS、日期原数值、格式、特殊缺失、分块CSV一致性及中文输出路径。来源哈希及MIT许可证保留。样例不含本研究患者，未覆盖所有CNSR-III SAS编码及损坏文件。
2. 已知二值掩膜的物理体积、单位及非二值拒绝；真实702标签；全WMH不等于20区域和；可选病灶失败不删除主WMH。
3. 字符串ID、重复ID拒绝、延迟入组、入组后90天内事件、同日边界、竞争死亡、失访删失和实际3个月采血起点。
4. Rubin方差、限制性样条线性尾部、零/已知交互以及竞争风险积分的解析例。
5. 插补保留观察值和分类编码；整列未测不插补；每轮均值轨迹输出。6. 四假设执行顺序、析取原因准则、全协方差对比、M3基线非线性及交互保留、固定三项Holm均有测试。

## 有限数值模拟

每个交互场景20次、每次生成1000人；只用于数值实现检查。

|真实交互系数|平均估计|偏差|95%区间覆盖率|
|---|---|---|---|
|0.00|0.0077|0.0077|85%|
|0.65|0.6805|0.0305|90%|

各场景仅20次，覆盖率Monte Carlo误差较大；不能将本次85%–90%覆盖率称为已证明95%校准，也不能作为真实研究的效能分析。

另做10个MAR检查：设置与Hcy相关且影响结局的B12，再引入依赖Hcy和事件状态的B12缺失。每例5份插补、5轮。

|真实交互|完整数据平均估计|插补平均估计|插补－完整数据平均差|
|---|---|---|---|
|0.00|-0.0409|-0.0418|-0.0009|
|0.65|0.6574|0.6599|0.0025|

这是一个有限MAR场景的压力检查。它不验证MNAR、所有非线性或复杂缺失组合，也不证明miceforest与Cox完全相容。

## PPT检查

28页均已渲染并逐页检查，修正了箭头方向、参考文献重叠及换行。原生表格位于第5、9、14页，路线和因果图为可编辑形状及连接线。包结构与版式检查均为0项问题；Artifact Tool重新导入成功。未执行桌面PowerPoint原生渲染。
WSL共享路径不支持验证工具最终发布所用的硬链接。因此仅在Windows临时目录验证同字节PPT，再按SHA-256回写WSL。项目和最终交付仍在指定WSL目录。

PPT SHA-256：`3075b04e037a69ec74fb59bc6613a9c4a920fa67d31a322ea2b2a7423046df8e`。

## 尚未验证

患者级单位和编码、日期真实含义、每份SAS的读取、影像与临床ID实际匹配、纳入样本和事件数、缺失机制、模型假设及临床估计精度。没有进行患者数据分析。
真实配置为50份插补、每份200次bootstrap。合成演示只用2份、每份3次，不能将演示区间作为研究精度。
模型假设和真实数据质量需要在接入后审阅。

## 重现命令

```bash
uv run pytest -q --junitxml=outputs/validation/pytest.xml
uv run ruff check src tests scripts
uv run python scripts/validate_simulations.py
uv run python scripts/validate_imputation.py
uv run wmh-hcy demo --n 700
```
